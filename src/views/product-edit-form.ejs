<!DOCTYPE html>
<html lang="es">
<%- include('partials/head', {title: 'Editar: ' + '	EDIT'}) %>
<body>
	<%- include('partials/headerNavbar') %>

	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Estas editando este producto : <i>PRODUCT NAME</i></h2>
			</div>
		</div>
		<div class="col-12">
			<form action="/products/editar/<%= producto.id %>?_method=put" method="POST" enctype="multipart/form-data">
				<div class="row product-detail">
					<div class="col-12 col-md-6">
						<label for="name" class="form-label">Nombre del producto:</label>
						<input type="text" id="name" name="name" placeholder="Ej: Zapatillas Reebok" class="form-input" value="<%= producto.name %>">
					</div>
					<div class="col-12 col-md-6">
						<label for="price" class="form-label">Precio del producto:</label>
						<input type="text" id="price" name="price" placeholder="Ej: 5678" class="form-input" value="<%= producto.price %>">
					</div>
					<div class="col-12 col-md-6">
						<label for="discount" class="form-label">Descuento:</label>
						<input type="text" id="discount" name="discount" placeholder="Ej: 15" class="form-input" value="<%= producto.discount || "no hay descuentos" %>">
					</div>
					<div class="col-12 col-md-6">
						<label for="category" class="form-label">Categoría:</label>
						<select name="category" id="category" class="form-input">
							<option <%= producto.category  === null && "selected"  %> value="Elegi">Elegí</option>
							<option <%= producto.category  === "in-sale" && "selected" %> value="in-sale">En oferta</option>
							<option <%= producto.category === "visited" && "selected" %> value="visited" >Últimos agregados</option>
						</select>
					</div>
					<div class="col-12">
						<label for="description" class="form-label">Descipción:</label>
						<textarea name="description" id="description" class="form-input"><%= producto.description %> </textarea>
					</div>
					<div class="col-12 col-md-6">
						<label for="images" class="form-label">SUBIR IMAGEN:</label>
						<input name="images" type="file" id="images">
					</div>
					<div class="col-12">
						<button type="submit" class="buy-now-button">Guardar producto</button>
					</div>
				</div>
			</form>
		</div>

	</div>

	<%- include('partials/footer') %>
</body>	
</html>